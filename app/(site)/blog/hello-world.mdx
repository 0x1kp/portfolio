export const metadata = {
  title: 'Hello World',
  description: 'First MDX post',
}

# Hello World

This is a **sample MDX** post in the clean-room portfolio scaffold.

- Written in MDX
- Rendered through the App Router
- Styled by Tailwind's `prose` classes if you wrap this in a `prose` container
